{
  "name": "api-server-example",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "lint": "tsc --noEmit -p tsconfig.json && prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "fastify": "4.4.0"
  },
  "devDependencies": {
    "@types/node": "18.7.3",
    "prettier": "2.7.1",
    "ts-node-dev": "2.0.0",
    "typescript": "4.7.4"
  }
}
